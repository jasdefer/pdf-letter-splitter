# Docker Compose profile (cpu or gpu)
# Use 'cpu' for CPU-only mode (default)
# Use 'gpu' for GPU-accelerated mode
COMPOSE_PROFILES=cpu

# Input PDF file (must exist in Source/)
INPUT_PDF=input.pdf

# Output TSV file
OUTPUT_TSV=output.tsv

# LLM model file (must exist in Source/)
MODEL_FILE=model.gguf

# Optional OCR settings (can be extended later)
OCR_JOBS=0
